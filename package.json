{
  "name": "agenticchat",
  "version": "1.0.0",
  "description": "A lightweight browser-based chat interface that turns natural language prompts into executable JavaScript, powered by OpenAI GPT-4o.",
  "main": "app.js",
  "browser": "app.js",
  "files": [
    "app.js",
    "index.html",
    "style.css",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build:sw": "node -e \"const fs=require('fs');const h=require('crypto').createHash('sha1');['index.html','app.js','style.css'].forEach(f=>h.update(fs.readFileSync(f)));const hash=h.digest('hex').slice(0,10);let sw=fs.readFileSync('sw.js','utf8');sw=sw.replace(/agenticchat-v[\\w]+/,'agenticchat-v'+hash);fs.writeFileSync('sw.js',sw);console.log('sw.js cache key: agenticchat-v'+hash);\"",
    "test": "jest --verbose",
    "test:coverage": "c8 --include=app.js --reporter=text --reporter=text-summary --reporter=lcov --reporter=json-summary npx jest --verbose",
    "prepublishOnly": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sauravbhattacharya001/agenticchat.git"
  },
  "keywords": [
    "ai",
    "chat",
    "openai",
    "gpt-4o",
    "javascript",
    "sandbox",
    "code-execution",
    "browser",
    "agentic"
  ],
  "author": "Saurav Bhattacharya",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sauravbhattacharya001/agenticchat/issues"
  },
  "homepage": "https://github.com/sauravbhattacharya001/agenticchat#readme",
  "devDependencies": {
    "c8": "^11.0.0",
    "jest": "^30.3.0",
    "jest-environment-jsdom": "^30.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
